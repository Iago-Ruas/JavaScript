const frase = `Eu moro em Salvador.`

function inverterString(texto){
    let arrayDoTexto = texto.split(``);
    arrayDoTexto.reverse();
    console.log(textoModificado.join(``));
}

inverterString(frase);