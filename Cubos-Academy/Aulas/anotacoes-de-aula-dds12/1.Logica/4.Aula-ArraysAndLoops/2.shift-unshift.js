const nomeDeEstudantes = ['josé', 'joão', 'maria', 'joana', 'andreia', 'alice'];

nomeDeEstudantes.unshift('iago'); // insere o item 'iago' no início do array

console.log(nomeDeEstudantes);

nomeDeEstudantes.shift(); // retira o ultimo item do início do array

console.log(nomeDeEstudantes);